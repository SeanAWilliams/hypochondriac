/**
 * Created by william on 12/29/2015.
 */

var model = require('../model/disease.js');

/*
 * (survey->disease )
 *
* var surveyToDisease(survey) {
    var d = {}
    if (survey.rectalBleeding == true) {
      d.timeToLive = -1;
    }

    return d;
}
}
 */
var getDisease = function(questions){
    var disease = new model.diseaseSchema();

    return disease;
};

var getName = functrion